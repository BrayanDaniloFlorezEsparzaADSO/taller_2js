addEventListener(`DOMContentLoaded`,()=>{
    // # DEFINIR VARIABLES NUEMRIAS
    // # DATOS DE ENTRADA
    // ## OPERACIONES BASICAS: + /
    let madre=Number(prompt(`edad de la Mamá:`))
    let hijo=Number(prompt(`edad del hijo:`))
    // #PROCESOS U OPERACIONES
    let res=(madre-hijo)
    // #salida o entrada de resultado
    console.log(`edad de la madre en el parto: ${res}`);
})