addEventListener(`DOMContentLoaded`,()=>{
    // # DEFINIR VARIABLES NUEMRIAS
    // # DATOS DE ENTRADA
    // ## OPERACIONES BASICAS: + /
    let grch=Number(prompt(`ingrese el numero de gr de el chocolate`))
    let grharina=100
    let grcacao=10
    let pnuez=1
    // #PROCESOS U OPERACIONES
    let res=grharina-grch
    // #salida o entrada de resultado
    console.log(`la cantidad de gramos de harina es de :${res}`);
})